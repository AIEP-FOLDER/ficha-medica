# Puerto del servidor (opcional, por defecto 3000)
PORT=3000

# URI de conexión a MongoDB
# Para MongoDB local:
MONGODB_URI=mongodb://localhost:27017/ficha-medica

# Para MongoDB Atlas (reemplaza con tu connection string):
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/ficha-medica?retryWrites=true&w=majority

# Configuración adicional (opcional)
NODE_ENV=development
